**C:\\work\\payroll\_app\\dist**の中にある、exeファイルさえあれば動く。

他のデータはexeを作成する際のコードなので、運用には不要。



**利用者は、データ保存先フォルダだけ作成すればよい。**



**Test環境**では、Power Shellを開き、

(.venv) PS C:\\work\\payroll\_app>  の環境で作業実行

&nbsp; **"python .\\app.py"**





**★exe化**　～PyInstallerで schema.sql を同梱して exe化～

**①まず古いビルド成果物を消す（失敗を減らす）**

（同じフォルダに dist や build が残っていると混乱しやすいので消します。）



PowerShellで：



cd C:\\work\\payroll\_app

Remove-Item -Recurse -Force .\\dist -ErrorAction SilentlyContinue

Remove-Item -Recurse -Force .\\build -ErrorAction SilentlyContinue

Remove-Item -Force .\\app.spec -ErrorAction SilentlyContinue



**②exeを作る（schema.sql を同梱）**



同じPowerShellでこれを実行します：



pyinstaller --onefile --noconsole --add-data "schema.sql;." app.py



超重要（Windowsの区切り）

Windowsでは --add-data "ファイル;配置先" の区切りが ; です。

（macは : なので注意）



**③起動して動作確認**

以下に格納される

C:\\work\\payroll\_app\\dist\\app.exe



